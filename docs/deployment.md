# Deployment

This repository is connected to Cloudflare Workers & Pages.

- Production branch: `main`
- Deploy command: `npx wrangler deploy`
- Root directory: `/`

Commits to `main` can trigger Cloudflare builds and deployments.
